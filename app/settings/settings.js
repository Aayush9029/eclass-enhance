/* the behavior of the settings page */
$(document).ready(function() {

    /* dark mode */
    $('#dark').click(function() {

    });


    /* serif fonts */
    $('#serif').click(function() {

    });

});


// var crnt_val = STYLES.get("/added_styles/serif.css");

// /* set it the negation of its current value */
// STYLES.set("/added_styles/serif.css", !crnt_val);

// console.log(STYLES);

// /* reloads the page */
// location.reload();